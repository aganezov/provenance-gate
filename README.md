# provenance-gate

A deterministic, claim-level **trust gate** for agentic science, running read-only over Claude Science.

Trust-critical checks are deterministic code; everything a model proposes must be human-confirmed before it counts. Green means a reported value *faithfully reflects* a computation — never that the analysis was correct.

> Status: setup / scaffolding. Concept docs and build plan to follow.
